# AGENTS.md

## ????

- **??**?`langchain_api/`?FastAPI + LangGraph/DeepAgents?Python 3.12?
- **API ?**?`langchain_api/api/`??? FastAPI ????????????
- **Agent ?**?`langchain_api/agent/`??? Agent ???????????????
- **RAG ?**?`langchain_api/rag/`??? Elasticsearch ?????? RAG?RAG ?????????
- **???**?`langchain_api/middleware/`????????RAG ????????????????
- **??**?`langchain_api/tools/`???????????????????
- **???**?`frontend/`?Next.js + React?????? FastAPI ? `/` ???????????
- **???**?`.langchain_api/workspace`?? `langchain_api/constant.py` ???

## ????

```bash
# ?????
cp .env.example .env
uv sync --dev

# ??????? 7869
uv run uvicorn langchain_api.agent.main:app --reload --host 0.0.0.0 --port 7869

# ?????
cd frontend
pnpm install
pnpm dev      # http://localhost:3000
pnpm lint

# ?? Next.js ???????? frontend/out
pnpm build

# Phoenix ???????
docker-compose up -d phoenix  # http://localhost:6006
```

## ?????

`langchain_api/agent/main.py` ?? FastAPI ???

- `/`??? `frontend/out`????? Next.js ???????
- `/api/ag_ui`?AG-UI ??????? `LangGraphAGUIAgent`?
- `/api/general_api`????????SSE ???

????????????? `frontend/` ??? `pnpm build`??????? `/` ?????????

## ????

`.env` ???

- `OPENAI_API_BASE`?`OPENAI_API_KEY`?LLM ????????
- `CHAT_MODEL_NAME`?????????? `qwen3`?
- `EMBEDDING_MODEL_NAME`?????????? `qwen3-embedding`?
- `ES_URL`?`ES_URSR`?`ES_PWD`?Elasticsearch ?????

???

- `BACKEND_TYPE`?`local_shell`?`store`?`sandbox`??? `local_shell`?
- `TAVILY_API_KEY`??????????
- `PG_DATABASE_URL`??? PostgresStore ??????
- `USE_TOOL_SEARCH=True`??????????
- `USE_COPILOTKIT=True`??? CopilotKit ????
- `PHOENIX_COLLECTOR_ENDPOINT`??? Phoenix tracing?

## ????

### Agent ??

`langchain_api/agent/agent.py`?

- `Agent` ???? DeepAgent???? ReactAgent?
- `BusinessMiddleware` ?? `internet_search`?`deep_thinking` ??????
- `DeferredToolMiddleware` ? `USE_TOOL_SEARCH=True` ????????
- ?? `local_shell`?`store`?`sandbox` ?????????

### API ?

- `langchain_api/api/endpoints.py`???????????? `/api/general_api`?
- `langchain_api/api/__init__.py`??? API ???????

### RAG ???

- `langchain_api/rag/retriever.py`??? Elasticsearch ??????? DenseVector/BM25 ?? RAG ?????
- `langchain_api/rag/elastic_utils.py`?Elasticsearch ???????????????????????
- `langchain_api/rag/elastic_graph_rag.py`??? ES ???? RAG?
  - `ElasticGraphRAG.add_texts()`????????????
  - `ElasticGraphRAG.add_documents()`?`Document` ?????????
  - `ElasticGraphRAG.retrieve()`?????/?????????????? passage ???
  - `ElasticGraphRAG.delete_documents()`???? ID ?? passage????????/???
  - `ElasticGraphRAG.delete_graph()`????????????

### ????

- `langchain_api/settings.py`??? `pydantic_settings` ?? `.env`?
- `langchain_api/utils.py`???????? embedding ???
- `langchain_api/constant.py`?????????????
- `langchain_api/patch/langchain.py`??? LangChain ?????????

## Sandbox ??

?? `.sandbox.toml` ?? OpenSandbox?

```bash
opensandbox-server --config .sandbox.toml
```

Sandbox ?? Playwright?

```bash
playwright install --with-deps chromium
```

## ??????

- ?? Python ?????????

```bash
uv run python -m py_compile <changed_file.py>
```

- ???????????

```bash
cd frontend
pnpm lint
pnpm build
```

- ???????????????????????????????
- ??????????????????????????????
